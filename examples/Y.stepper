y = \f -> (\x -> f (x x)) (\x -> f (x x))

fib = fib' 0 1

fib' = y \self -> \a b i ->
  case i of
    0 -> a
    _ ->
      let b' = Natural.add(a, b)
          i' = Natural.sub(i, 1)
      in self b b' i'

result = fib Natural.add(1, Natural.mul(2, 5))